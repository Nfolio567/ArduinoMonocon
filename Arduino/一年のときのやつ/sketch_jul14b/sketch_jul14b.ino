   pinMode( nasg[0], OUTPUT );//一番上
   pinMode( nasg[1], OUTPUT );//右上
   pinMode( nasg[2], OUTPUT );//右下
   pinMode( nasg[3], OUTPUT );//一番下
   pinMode( nasg[4], OUTPUT );//左下
   pinMode( nasg[5], OUTPUT );//左上
   pinMode( nasg[6], OUTPUT );//真ん中
   pinMode( nasg[7], OUTPUT );//点
   pinMode( nasgvcc[0], OUTPUT );
   pinMode( nasgvcc[1], OUTPUT );
   pinMode( photoic, INPUT);
